import { SourceDirective } from './source.directive';

describe('SourceDirective', () => {
  it('should create an instance', () => {
    const directive = new SourceDirective();
    expect(directive).toBeTruthy();
  });
});
